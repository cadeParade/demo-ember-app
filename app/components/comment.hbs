<div class="flex w-full items-center font-sans p-4">
  <img class="w-10 h-10 rounded-full mr-4" src="{{@comment.author.avatarUrl}}" alt="Avatar of Author">
  <div class="flex-1 px-2">
    <p class="text-base font-bold text-base md:text-xl leading-none mb-2">{{@comment.author.name}}</p>
    <p class="text-gray-600 text-xs md:text-base">{{@comment.body}}</p>
  </div>
</div>

