import { JSONAPISerializer } from "ember-cli-mirage";

export default JSONAPISerializer.extend({
  // links(post) {
  //   return {
  //     comments: {
  //       related: `comments`,
  //       // related: `comments?post_id=${post.id}`,
  //     },
  //   };
  // },
});
